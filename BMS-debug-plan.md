# SLMC607 BMS 调试计划

> 项目: SLMC607 V1.6 电池管理系统
> MCU: STM32F103C6 | AFE: BQ76920/BQ76940
> 日期: 2026-03-31

---

## 调试准备

### 硬件设备
- [ ] SLMC607 BMS板
- [ ] ST-Link/J-Link调试器
- [ ] 电池组（3-5串）
- [ ] 充电器/负载
- [ ] 万用表/示波器

### 软件工具
- [ ] Keil MDK
- [ ] 串口调试助手（115200波特率）
- [ ] BMS上位机软件

---

## 调试计划（10个阶段）

### 阶段1：硬件连接检查 📌

| 检查项 | 方法 | 预期结果 |
|--------|------|----------|
| 电源电压 | 万用表测量VCC | 3.3V |
| ST-Link连接 | Keil Debug→Connect | 连接成功 |
| 电池电压 | 万用表测量各串 | 3.0-4.2V/串 |
| I2C上拉 | 示波器观察SCL/SDA | 有上拉电阻 |

**完成标志**: `[ ]` 硬件检查全部通过

---

### 阶段2：GPIO和基础外设验证 🔧

**测试代码位置**: `Source/Drive/gpio.c`

| 测试项 | 测试方法 | 预期结果 |
|--------|----------|----------|
| LED闪烁 | `Led_Run_Cpu()` | LED周期闪烁 |
| WAKE引脚 | 拉高1秒后测量 | BQ769xx唤醒 |
| ALERT读取 | 读取GPIO状态 | 能读取告警 |
| RS485方向 | 切换收发控制 | 能切换方向 |

**调试命令**:
```c
// 在main.c中添加测试
GPIO_WriteBit(LED_GPIO, LED_PIN, 1);
Delay_ms(500);
GPIO_WriteBit(LED_GPIO, LED_PIN, 0);
```

**完成标志**: `[ ]` LED正常闪烁，GPIO控制正常

---

### 阶段3：I2C通信验证 📡

**测试代码位置**: `Source/Drive/iic.c`

| 测试项 | 测试方法 | 预期结果 |
|--------|----------|----------|
| I2C扫描 | 扫描0x08地址 | 能检测到设备 |
| 读寄存器 | 读0x00 SYS_STAT | 返回有效数据 |
| 写寄存器 | 写0x05 SYS_CTRL2 | 写入成功 |
| CRC校验 | 验证CRC8计算 | 校验通过 |

**调试命令**:
```c
// 在BQ769xx_Init()中添加
u8 stat = IIC_ReadByte(0x08, 0x00);
printf("SYS_STAT: 0x%02X\n", stat);
```

**预期输出**: `SYS_STAT: 0xXX` (非0xFF)

**完成标志**: `[ ]` I2C读写正常，CRC校验通过

---

### 阶段4：BQ769xx初始化验证 ⚙️

**测试代码位置**: `Source/App/bq769xx.c:98-250`

| 测试项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| CellMap写入 | 读回0x01-0x03 | 与写入一致 |
| 保护参数配置 | 读回0x06-0x0A | 配置正确 |
| Gain/Offset读取 | 读0x50-0x5D | 获得校准值 |
| 配置验证 | BQ769xx_Init_State | = 1 |

**调试输出**:
```c
printf("CellMap: 0x%04X\n", gBMSConfig.Type.Cells_Map);
printf("Gain: %lu uV, Offset: %ld uV\n", gain, offset);
printf("Init_State: %d\n", BQ769xx_Init_State);
```

**完成标志**: `[ ]` BQ769xx_Init_State = 1，配置验证通过

---

### 阶段5：电压采集验证 🔋

**测试代码位置**: `Source/App/bq769xx.c:327-414`

| 测试项 | 测试方法 | 预期结果 |
|--------|----------|----------|
| 各串电压 | 与万用表对比 | 误差<50mV |
| 总电压 | 各串之和 | 一致 |
| 更新频率 | 200ms时间片 | 稳定采集 |

**调试输出**:
```c
for(i=0; i<CellNum_Ser; i++) {
    printf("Cell%d: %dmV ", i+1, gBMSData.BattPar.VoltCell[i]);
}
printf("Total: %dmV\n", gBMSData.BattPar.VoltLine);
```

**完成标志**: `[ ]` 电压采集准确，误差在允许范围

---

### 阶段6：电流采集验证 ⚡

**测试代码位置**: `Source/App/bq769xx.c:256-325`

| 测试项 | 测试方法 | 预期结果 |
|--------|----------|----------|
| 静置电流 | 无充放电 | -20~20mA |
| 充电电流 | 接充电器 | 正电流 |
| 放电电流 | 接负载 | 负电流 |
| 符号方向 | 验证正负 | 充正放负 |

**调试输出**:
```c
printf("Current: %dmA\n", gBMSData.BattPar.CurrLine);
```

**完成标志**: `[ ]` 电流方向正确，读数合理

---

### 阶段7：温度采集验证 🌡️

**测试代码位置**: `Source/App/bq769xx.c:256-325`

| 测试项 | 测试方法 | 预期结果 |
|--------|----------|----------|
| NTC读数 | 与温度计对比 | 误差<3℃ |
| 温度范围 | 常温测试 | 20-30℃ |
| 传感器数量 | TS1/TS2/TS3 | 对应配置 |

**调试输出**:
```c
printf("TS1: %d.%d℃ TS2: %d.%d℃ TS3: %d.%d℃\n",
    temp1/10, temp1%10, temp2/10, temp2%10, temp3/10, temp3%10);
```

**完成标志**: `[ ]` 温度采集准确

---

### 阶段8：系统模式状态机验证 🔄

**测试代码位置**: `Source/App/TaskFun.c:40-74`

| 测试项 | 测试方法 | 预期结果 |
|--------|----------|----------|
| 待机模式 | 静置状态 | STANDBY |
| 充电模式 | 接充电器(>20mA) | CHARGE |
| 放电模式 | 接负载(<-20mA) | DISCHARGE |
| 睡眠模式 | 静置2小时 | SLEEP |

**调试输出**:
```c
printf("Mode: %d, Curr: %dmA\n",
    gBMSData.Sys_Mod.sys_mode,
    gBMSData.BattPar.CurrLine);
```

**模式判断**:
```
0=SLEEP, 1=STANDBY, 2=DISCHARGE, 3=CHARGE
```

**完成标志**: `[ ]` 模式切换正确，响应及时

---

### 阶段9：保护功能验证 🛡️

**测试代码位置**: `Source/App/bq769xx.c:113-176`

| 保护项 | 触发条件 | 预期结果 |
|--------|----------|----------|
| 过压(OV) | 单串>4.2V | CHG MOS关 |
| 欠压(UV) | 单串<2.7V | DSG MOS关 |
| 短路(SCD) | 电流突增>200A | 双MOS关 |
| 过温(OTP) | 温度>60℃ | 双MOS关 |

**调试输出**:
```c
printf("SYS_STAT: 0x%02X\n", stat);
printf("CHG_PD: %d, DSG_PD: %d\n",
    gBMSData.StatePar.CHG_PD,
    gBMSData.StatePar.DSG_PD);
```

**完成标志**: `[ ]` 保护功能正常，MOS能正确开关

---

### 阶段10：均衡功能验证 ⚖️

**测试代码位置**: `Source/App/bq769xx.c:841-984`

| 测试项 | 测试方法 | 预期结果 |
|--------|----------|----------|
| 均衡启动 | 压差>100mV | 开启均衡 |
| 均衡选择 | 选择高电压串 | 正确选择 |
| 均衡计时 | 180秒后停止 | 时间准确 |
| 均衡效果 | 测量电压变化 | 压差减小 |

**调试输出**:
```c
printf("BalState: %d, BalSw: 0x%02X\n",
    gBMSData.BalaPar.bala_state,
    gBMSData.BalaPar.bala_ctrl_sw);
```

**状态**: 0=INIT, 1=WAIT, 2=CHK, 3=SELECT, 4=MAP, 5=SET, 6=BALA_TIM

**完成标志**: `[ ]` 均衡功能正常，压差减小

---

### 阶段11：RS485通信验证 📡

**测试代码位置**: `Source/App/Rs485Data.c`

| 测试项 | 测试方法 | 预期结果 |
|--------|----------|----------|
| 数据发送 | 上位机发送命令 | 收到响应 |
| 数据解析 | 解析电压/电流 | 数据正确 |
| 告警上报 | 触发保护 | 上报告警 |

**测试命令**: 发送读取电压命令
```
帧头 | 地址 | 命令 | 长度 | 校验 | 帧尾
```

**完成标志**: `[ ]` 通信正常，数据上报正确

---

## 调试记录模板

### 日期: _____

| 阶段 | 测试项 | 结果 | 问题 | 解决方案 |
|------|--------|------|------|----------|
|      |        | PASS/FAIL |      |          |

---

## 常见问题速查

| 问题 | 可能原因 | 解决方法 |
|------|----------|----------|
| I2C通信失败 | 硬件连接/地址错误 | 检查SCL/SDA/地址 |
| 电压读数为0 | BQ769xx未唤醒 | 检查WAKE引脚 |
| MOS无法控制 | 保护未清除 | 调用STAT_CLEAR |
| 模式不切换 | 电流阈值不对 | 检查20mA阈值 |

---

## 完成标准

- [ ] 所有11个阶段测试通过
- [ ] 调试日志记录完整
- [ ] 问题都有解决方案记录
- [ ] Git提交记录清晰

---

*调试计划版本: V1.0*
*制定日期: 2026-03-31*
